# DEV ONLY — vite injects these only in `vite dev` mode.
# Placeholder token for open local dev servers. Real tokens go in your
# local (uncommitted) .env.local or ~/.mira/mira.env. Never commit real credentials.
VITE_MIRA_TOKEN=dev-local-token-not-secret
VITE_API_URL=/
