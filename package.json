{
  "name": "mira",
  "version": "0.1.0",
  "description": "AI agent platform — openness + reasoning + polish + autonomy + transparency, plus memory/eval/guardrails",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "eval": "turbo run eval"
  },
  "devDependencies": {
    "turbo": "2.10.11",
    "typescript": "5.9.3"
  },
  "packageManager": "bun@1.2.0"
}